<h1>Admin: <%= @user.email%></h1>
<table>
	<tr>
		<th>ID</th>
		<th>Email</th>
	</tr>
	<% @users.each do |u|%>
	<tr>
		<td><%= u.id%></td>
		<td><%= u.email%></td>
		<td><%= link_to "view", admin_path(u)%></td>
	</tr>
	<% end %>
</table>
<%= link_to "Logout", logout_path, method: :delete%>